FIRST!! You need to install python interperter from python's official page otherwise nothing works 

To run the application just type "flask run" in the terminal make sure you installed flask by typing
"pip install flask" in the terminal
